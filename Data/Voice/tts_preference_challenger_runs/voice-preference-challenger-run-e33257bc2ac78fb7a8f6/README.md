# Private voice-preference challenger run

This directory contains private candidate mapping and Provider audit records. Never copy it into a public review package. An attempt without a matching result blocks automatic continuation.
