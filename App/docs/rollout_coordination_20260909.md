# 角色发布与本地语音接入统筹

用户于 2026-09-09 指示：监控「迁移任务并绘制角色表情」，完成后开始上线；同时将 TTS 相关功能上线到本地测试软件。这是本批交付完成后的执行授权，仍需确切版本的技术验收，不需要重复询问泛化上线许可。

## 任务与责任

| 工作 | 执行范围 | 当前证据 |
| --- | --- | --- |
| 角色素材 | `01a07005-75c4-75f2-ac5a-a53d27df8736` 已交付，统筹接管 PR #52 与部署 | `9e50a648f2718e7c1f29021e0dd88c20d1ba6eef`、CI 34303066734 成功；22 角色、396 基础状态、60 特殊表现，1468 个文件重新验哈希通过；正在补浏览器完整性门禁并组合公开包 |
| 本地 TTS | `codex/local-voice-test` 独立工作树实现；cd1e 原素材只读 | 本地提交 `117ff4f8ce69127dc328e19cd4ddcb5d58ca5198`；23 个声线/变体、138 条试听已运行交付；实际 Windows 隐藏启动、重复打开与 Chrome 播放/停止/下载通过；新增合成等待账户币种确认 |
| 公网切换 | 本统筹任务 `01a07aae-e207-7c71-a61b-91fef78f6d88` | S2 先前验收记录仍单独保留；角色任务不得并发覆盖候选或执行 stage/promote |

已创建本任务的「小吉终端角色交付与上线跟进」心跳，ID `automation`，每 20 分钟检查。无变化时保持安静，交付完成、失败或需要用户处理时通知。任务状态 idle 不等于交付完成；以确切提交、产物和验收记录为准。两项完成并记录上线后验证时暂停此心跳。

## 角色完成后的执行顺序

1. 接收不可变素材清单、U4 批准记录、22 角色覆盖、编码后实际浏览器检查及确切 PR/CI。核对发布权限记录的真实来源和用户最新授权，不能伪造第三方许可。
2. 将已审交付接入当前主线；核验实际前端资源树、回退、减少动态及失败素材处理。
3. 重新构建并验证发布证明、镜像和媒体摘要，检查容量及当前稳定版本独立恢复锚点。
4. 根据服务器当时实际版本准备候选并验收。旧 S2 回执 `85d591646eb619956aceebcd73a9c50f8e4c34bafaecb17d95ca78284a33d92a` 至 2026-09-09 07:36:18 UTC 有效，版本变化或过期均须重新验证。
5. 按本次明确授权晋级，核对公网运行身份、入口代理、桌面/窄屏、草稿恢复、健康和备份；失败时执行已验证的应用回退，保留新增业务数据。

## 本地 TTS 范围

独立服务只监听 `127.0.0.1`，提供角色选择、已选试听、文本合成、播放/停止/下载、功能开关及用量记录。私人 API 均认证并校验 Host/Origin；密钥和真实音色 ID 留在进程及私有配置中。公网 API、镜像与角色素材包不包含私人语音入口或映射。

每次启动默认关闭付费生成。复用原账户与已选音色，保留真实供应商适配器；普通测试不发付费请求。请求持久防重，同一结果缓存重播无新增调用，超时或结果未知保留预算占额并停止自动重试。

本地初始额度采用每日 ¥5、累计 ¥20，并计入原始 ¥300 总预算；原 ¥30 保护预留保持。现有账户配置只证明北京地域和固定模型，不能证明账单币种。账单币种已向用户核实，未确认时保留已有试听可用，不能伪报付费功能已完成验收。

原共享账本新增两条兼容的容量分配记录，前 674 条保持逐字节不变。分配 ID `7e8c840e-1a6a-45aa-b61b-1251d9ecd78a` 把 ¥20 绑定到唯一运行目录和账本实例；这是预算预留，新增供应商费用为 0，不等于已经支付 ¥20。不可变分配回执摘要为 `efeb8ab55fa3fe4ece0874ca98d4bb7db77628171f41de3f3ffae30c533ad8cb`。本地实际账本中尚无合成请求。

交付入口是桌面「小吉终端 - 本地语音测试」快捷方式，指向独立工作树私有运行目录中的 `Open-Local-Voice.cmd`。服务只监听 `127.0.0.1:8769`；启动器通过会话身份核对后打开已有实例，不在命令行输出访问令牌。当前实例已经在用户浏览器打开。47 项测试通过，实机报告 `browser-qa-20260909-r4/report.json` 摘要为 `1476ca3e8394565b1c7246ede1020a77176833ef617e05aecdbff74ed5a87711`：1365px/320px 均可用，实际播放与下载的 364844 字节 WAV 一致；POST 与外部请求尝试均为 0。截图包含原审核备注，只保留在 Git 忽略的私有目录。

本地 GPG 签名等待超时后，该 13 文件提交按统筹决定仅本次未签名，原签名配置保持；尚未推送或合并 TTS 分支。公网发布的完整 CI、镜像扫描与 GitHub 发布证明验证仍是独立必要门禁。

## 当前公开发布准备

U4 技术交接清单摘要为 `ce217f825004b7e1cb70335824cfd117fa3e66426420e8ccce14c73a0997b62b`，用户视觉批准事件摘要为 `3d97067f097c54efcea96b4afc38e3fd75007c4931aeed8616b5030372491342`。私有素材包的 manifest / SHA256SUMS 摘要分别为 `492deee4ed4c447495036f69953e3b32bf31f6ea5323c070b21b223caccf492a` / `393e755b66343460dfa338bc7b51d897ce7a374dba6bc97299331926d89613f0`，原包保持 `pending_not_public`。

本批公开包计划版本 `2026.09.09.character.1`，应用与当前前端为 `0.10.0-rc.2`。已重新读取并验证在线旧头像包的 46 张 WebP，保留原图像与署名；新包仅在新的目录中加入 U4 内容。用户本轮明确授权和此前规则覆盖决定记录为本批素材的公开运行授权及独立权利核验豁免，不声称获得第三方许可。公开授权记录摘要为 `850ded196a96a4af391d901262be61a1a0d6294248eda1218b68e7fc5420e011`。

接入复审发现角色 manifest 与图片此前仅检查格式或尺寸，缺少浏览器端 SHA 校验。独立修复为可信目录 → manifest 原始字节 → 每张图片原始字节的校验链，并补总期限、解码取消和已批准素材回退。打包复审另要求拒绝未知私有元数据，防止本机路径或操作备注混入公开清单。这些修复通过验收之前不合并发布。

2026-09-09 最近只读预检中，公网仍为 S1 `7ce0205 / green`，候选仍为 `c350963 / blue`，控制器工作区无未提交变化，25 个容器、可用磁盘约 16.08 GiB。尚未 stage 或 promote U4。上线之后再追加精确主线、镜像、媒体、验收回执与运行观察证据；不能把本页准备记录视为发布完成。
