# U4 立绘加载与回退修复

本页保留问题发现与修复准备过程；已完成的 rc.3 上线、正式回退验证和固定备份见[最终执行记录](u4-rc3-release-20260909.md)。

2026-09-09，`4d89d4e79c2e0758dcbee605ec1d4f41cea42434 / 0.10.0-rc.2`
完成候选验收和正式切换，但公网复验未完成。不能将该批记录视为完整上线验收。

公网首页与来源包的差异已定位为 Cloudflare 在 HTML 末尾插入的统计和浏览器检测
脚本。两次只读诊断均确认应用原文逐字节保持，来源 HTML 摘要仍为
`667391d9b383cea66762607a361fb3405083c2f21cb266d42d0ffb0052001e06`。
原始响应和首次失败报告均保留，公网复验需要分别验证应用内容和严格限定的边缘
插入内容，并向浏览器原样传递实际 HTML 与 CSP。

进一步复审发现实际产品缺陷：已校验图片通过 Blob URL 解码，而现有生产策略
`img-src 'self' data:` 禁止 Blob URL。此前媒体联系表只验证原始图像解码，未覆盖
实际舞台与该策略的组合。真实策略下的原版 single、layered 和 fallback 路径均已
复现图片不可见。修复应继续校验原始响应 SHA，采用已有策略允许的数据 URL，
并保留取消、超时和内存限制，不放宽线上 CSP。

正式回退至 S1 的首次尝试也失败：目标入口代理已与 S1 一致，但 `retain` 分支
沿用了颜色环境文件路径，最后又要求它等于已经保存的独立环境文件路径。内容
相同、路径不同会错误拒绝切换；回退脚本恢复了 U4 路由。需要使用入口代理已经
保存的实际运行配置，保留原有实例、TLS、安全检查及最终一致性检查。

截至本记录的准备阶段，公网仍为 U4/blue，S1/green 已启动但未接收公网流量。
API 存活与构建身份读取通过；未还原数据库，未新增模型或语音费用。固定 U4
备份只完成前置盘点，尚未执行。前置检查保护了 5 份既有固定快照。

修正版本使用 `0.10.0-rc.3`，继续复用同一已批准媒体包
`2026.09.09.character.1`。必须通过真实生产 CSP 下的舞台行为回归、控制器回退
回归、完整 CI、发布证明与新候选验收，才能完成后续发布。若单独维护发布
控制器，必须绑定完整 CI 已验证的精确主线，记录前后状态，再用正式回退入口
验证恢复能力；不能修改既有恢复环境文件来迁就错误路径检查。

实际修正提交、上线与恢复结果应在执行后追加，不能由本准备记录推断。

修复验证：原版在同一生产 CSP 下的三种舞台路径全部复现失败；修正后完整
前端 E2E 43 项及 21 个子场景通过，可靠性 15 项通过。其中有效、等尺寸且等
字节长度的 PNG 篡改在解码前被拒绝，已批准回退图正常显示。真实 Shell 回退
回归覆盖已保留入口、配置漂移及失败路径。集成版本的发布清单、前端包和部署
测试共 101 项、82 个子场景通过，Windows 环境跳过 3 项；Linux CI 另行验收。
